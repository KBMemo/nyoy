- content_for :title, "画像を生成"

.mb-6
  h1 class=kb_page_title 画像を生成

= render "shared/sd_catalog_status"

= form_with model: @image_generation, class: "space-y-4" do |form|
  - if @image_generation.errors.any?
    .kb-alert.kb-alert-danger
      ul.list-disc.pl-5
        - @image_generation.errors.full_messages.each do |message|
          li = message

  div
    = form.label :japanese_prompt, "日本語プロンプト", class: "#{kb_label} mb-1"
    = form.text_area :japanese_prompt, rows: 4, required: true, class: kb_field_input_classes, placeholder: "例: 鳥獣戯画風のウサギとカエルが相撲をとっている"

  div
    = form.label :negative_prompt, "ネガティブプロンプト (任意)", class: "#{kb_label} mb-1"
    = form.text_area :negative_prompt, rows: 2, class: kb_field_input_classes, placeholder: "low quality, blurry"

  = render "shared/generation_settings_fields",
    form_object: @image_generation,
    form_builder: form,
    generation_presets: @generation_presets,
    prompt_skills: @prompt_skills,
    sd_models: @sd_models,
    sd_loras: @sd_loras,
    sd_samplers: @sd_samplers,
    lora_catalog_error: @lora_catalog_error,
    show_preset_select: true,
    show_seed: true

  .flex.flex-wrap.items-center.gap-3
    = form.submit "生成する", class: kb_btn_primary_sm, disabled: @sd_models.empty?
    = link_to "プリセット管理", generation_presets_path, class: "kb-chrome-link text-sm"
    = link_to "戻る", image_generations_path, class: "kb-chrome-link text-sm"
